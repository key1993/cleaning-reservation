# Render.com Blueprint - ensures the app binds to PORT so the deploy is detected
services:
  - type: web
    name: cleaning-reservation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    healthCheckPath: /
